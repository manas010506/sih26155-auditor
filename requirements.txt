# Pinned. Nobody upgrades without telling the group.
# Check current versions on PyPI before the first commit, then freeze.

python-hcl2==7.2.1
ciscoconfparse2==0.8.6
scrypt==0.8.27
PyYAML==6.0.2
Flask==3.1.0
flask-cors==5.0.0
google-genai==1.38.0
pytest==8.3.4