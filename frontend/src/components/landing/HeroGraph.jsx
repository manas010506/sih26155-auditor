import React from 'react';
import DataMesh from './DataMesh';

// DataMesh renders as fixed, covering the entire viewport behind all content.
const HeroGraph = () => <DataMesh />;

export default HeroGraph;
